{
	"name": "b3-todo",
	"version": "1.0.0",
	"workspaces": ["front", "api", "tests"],
	"devDependencies": {
		"@biomejs/biome": "1.9.3",
		"concurrently": "^9.0.1"
	},
	"scripts": {
		"setup": "rm node_modules -rf && bun i && bun dev",
		"dev": "bun --filter '*' dev",
		"api": "cd api && bun dev:api"
	}
}
